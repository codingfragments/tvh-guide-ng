# Standard grid response definitions

GridSuccess:
  description: Successful grid response with paginated data
  content:
    application/json:
      schema:
        $ref: '../schemas/grid.yaml#/GridResponse'
      example:
        entries: []
        total: 0
        start: 0
        limit: 50
