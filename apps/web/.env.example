# Base URL of the epg-cache service
PUBLIC_EPG_CACHE_URL=http://localhost:3000

# Optional time preset buttons for TimeSelect (CSV "HH:mm,HH:mm,..." or JSON array)
PUBLIC_TIMESELECT_PRESETS=06:00,12:00,20:15,22:00
# Optional TimeSelect custom-time dropdown style: outline|ghost|primary|neutral
PUBLIC_TIMESELECT_CUSTOM_APPEARANCE=outline

# Optional: number of upcoming events shown on the channels page (default: 5)
CHANNELS_UPCOMING_LIMIT=5

# TVHeadend credentials used only by server-side live stream resolver/proxy
TVH_URL=http://localhost:9981
TVH_USERNAME=admin
TVH_PASSWORD=secret

# Optional: default stream profile used when no profile is explicitly selected
TVH_STREAM_DEFAULT_PROFILE=

# Optional: stream path template (default: /stream/channel/{channelUuid})
TVH_STREAM_PATH_TEMPLATE=/stream/channel/{channelUuid}
