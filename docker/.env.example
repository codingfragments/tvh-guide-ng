# ============================================================================
# tvh-guide-ng — Docker Environment Variables
# ============================================================================
# Copy this file to .env and fill in the values for your setup:
#   cp .env.example .env
# ============================================================================

# --------------------------------------------------------------------------
# TVHeadend Connection (REQUIRED)
# --------------------------------------------------------------------------

# URL of your TVHeadend instance
TVH_URL=http://192.168.1.10:9981

# TVHeadend credentials (leave empty if no auth is configured)
TVH_USERNAME=
TVH_PASSWORD=

# --------------------------------------------------------------------------
# EPG Cache Service
# --------------------------------------------------------------------------

# How often to re-sync EPG data from TVHeadend (seconds, default: 3600 = 1 hour)
EPG_REFRESH_INTERVAL=3600

# Host port for the EPG Cache API (default: 3100)
EPG_CACHE_PORT=3100

# --------------------------------------------------------------------------
# Web Frontend
# --------------------------------------------------------------------------

# Host port for the web UI (default: 8080)
WEB_PORT=8080

# URL the browser uses to reach the EPG Cache API.
# For most setups, use http://localhost:<EPG_CACHE_PORT> so the browser can
# reach the API. The default (http://epg-cache:3000) only works for
# server-side rendering within Docker.
PUBLIC_EPG_CACHE_URL=http://localhost:3100

# --------------------------------------------------------------------------
# Image Settings
# --------------------------------------------------------------------------

# Registry prefix for image names (include trailing slash)
# Default: ghcr.io/codingfragments/ (pre-built multi-arch images)
# Set to empty to use locally built images.
#   IMAGE_REGISTRY=
#   IMAGE_REGISTRY=docker.io/myuser/
IMAGE_REGISTRY=ghcr.io/codingfragments/

# Docker image tag (default: latest)
IMAGE_TAG=latest
